CREATE TABLE LOGIN_LOG
(
  CLIENTIP   VARCHAR2(200 BYTE),
  PNO        NUMBER,
  DATE1      DATE,
  SESSIONID  NUMBER,
  ATTRIB1    VARCHAR2(500 BYTE),
  SID        NUMBER,
  INST_ID    NUMBER,
  FULLIP     VARCHAR2(2000 BYTE)
);
